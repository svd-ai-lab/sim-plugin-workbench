"""Stub fixture: a Python file with no Ansys Workbench imports.

Used by detect()/lint() tests to confirm the driver does not claim
arbitrary `.py` files as Workbench scripts.
"""
print("hello — not a workbench script")
